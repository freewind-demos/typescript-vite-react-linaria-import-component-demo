TypeScript Vite React "linaria" Import Component Demo
=================================

linaria在build component时，会生成css文件。

相对于使用@emotion来说，其实多了一些不便，即使用者还需要手动import相应的css文件。

```
npm install
npm run demo
```

It will open page on browser automatically.
